# ==================================================
# .env.example - Environment Variables Template
# ==================================================
# 
# INSTRUCTIONS FOR USERS:
# 1. Copy this file and rename it to ".env"
#    Command: cp .env.example .env
# 
# 2. Fill in your actual values in the .env file
# 
# 3. Never commit the actual .env file to version control
#    Make sure .env is in your .gitignore file
#
# ==================================================

# ============================================
# GROQ API CONFIGURATION (REQUIRED)
# ============================================
# Get your FREE API key from: https://console.groq.com/keys
# Without this, the app will not work!
GROQ_API_KEY=

# ============================================
# MODEL CONFIGURATION
# ============================================
# Available models:
# - llama-3.1-8b-instant (Fast, good for most tasks) ⚡
# - llama-3.1-70b-versatile (More powerful, slower) 🧠
# - mixtral-8x7b-32768 (Large context window) 📚
MODEL_NAME=

# ============================================
# MODEL PARAMETERS
# ============================================
# Temperature: Controls randomness in responses
# - 0 = Deterministic (same answer every time) 🎯
# - 1 = Creative (different answers) 🎨
# Range: 0.0 to 1.0
TEMPERATURE=0

# Max Completion Tokens: Maximum length of generated response
# Higher = longer responses, but slower and more expensive
# Range: 100 to 8000 (depends on model)
MAX_COMPLETION_TOKENS=1024

# ============================================
# EMBEDDING MODEL CONFIGURATION
# ============================================
# HuggingFace embedding model for vector search
# 
# Recommended options:
# - all-MiniLM-L6-v2 (Fast, efficient, good for English) ⚡ [DEFAULT]
# - all-mpnet-base-v2 (Better quality, slower) 🎯
# - paraphrase-multilingual-MiniLM-L12-v2 (Multilingual support) 🌍
EMBEDDING_MODEL=

# ============================================
# TEXT SPLITTING CONFIGURATION
# ============================================
# Chunk Size: Number of characters per chunk
# Larger = more context, but less precise retrieval
# Smaller = more precise, but may miss context
CHUNK_SIZE=1000

# Chunk Overlap: Number of overlapping characters between chunks
# Helps maintain context across chunk boundaries
CHUNK_OVERLAP=200

# ============================================
# RETRIEVAL CONFIGURATION
# ============================================
# Number of relevant chunks to retrieve for answering questions
# More chunks = more context but slower response
# Fewer chunks = faster but may miss information
# Range: 1 to 10
RETRIEVAL_K=3

# ============================================
# WEB SCRAPING CONFIGURATION
# ============================================
# Default URL to show in the web scraping input box
# Users can change this in the app interface
DEFAULT_SCRAPE_URL=https://www.icmr.gov.in/tenders

# Request timeout in seconds
# How long to wait for website to respond before giving up
SCRAPE_TIMEOUT=10

# Minimum text length to include from scraped content
# Filters out very short text snippets
MIN_TEXT_LENGTH=30